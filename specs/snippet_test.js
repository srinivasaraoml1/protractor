describe('sample test for snippet', function() {




beforeEach(function() {

});

afterEach(function() {

});
it('sample test 1', function() {

  element(by.id());
  element(by.css());
  element(by.name());
  element(by.className());
  element.all(by.deepCss());
  element(by.linkText());
  element(by.buttonText());
  element(by.model());
  element(by.binding());
  element(by.xpath());

  element.all(by.id());
  element.all(by.css());
  element.all(by.name());
  element.all(by.className());
  element.all(by.deepCss());
  element.all(by.linkText());
  element.all(by.buttonText());
  element.all(by.model());
  element.all(by.binding());
  element.all(by.xpath());

});

});
