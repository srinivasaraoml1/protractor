module.exports = function(){

this.iptFirst = element(by.model('first'));
this.iptSecond = element(by.model('second'));
this.btnGo = element(by.id('gobutton'));

// the below locator is just for demo on usage of snippet
this.sample = element(by.id('sample_for_demo'));

}
